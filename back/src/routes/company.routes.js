/* import express from 'express';
import {registerCompany, addEmployee} from '../controller/user.controller.js';
import { revisarCookie } from '../middlewares/autorizacion.middleware.js';
const routerCompany = express.Router();


router.post("/registerCompany", revisarCookie, registerCompany);
router.post("/addEmployee", revisarCookie, addEmployee);

export default routerCompany;
 */

